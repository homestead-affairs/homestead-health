# Changelog

## 0.1.0

The initial standing release — module three on Homestead · Affairs, extracted
from the safe-app-store playground (the law-gazelle → homestead-law path).

### Added
- The records track (H-1..H-5): the roster (opaque subject ids), the immunizations
  pack (classified at import), due-onto-Today (calendar days, k>=2 re-identification),
  the school-form export (purposed S4 egress, dual-log, off-tree anchor), the pinned
  CDC/ACIP schedule, and the emergency card (authored, never computed).
- The 2026-08-17 extension: the living lane (a forgetting cell, H-8) and the
  reference lane (an injected reader over a pinned public-domain corpus, H-7).
- Twice adversarially audited (docs/audits/); 129 tests, 0 xfailed.
