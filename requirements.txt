# The one runtime dependency — the engine. Floor: the release this seat was
# verified against. Cap: `<1.0` — the engine's `bump-minor-pre-major: false`
# reserves 1.0.0 for its first compatibility break, so the floor moves release
# to release and a tighter, hand-argued cap only strands this module on
# minors Dependabot cannot lift (reversed 2026-09-11, H2-cap; see
# pyproject.toml's [project] dependencies for the fuller rationale). Mirrors
# that same line; the store's app-tests CI leg installs this file and runs
# the suite uninstalled (tests/conftest.py makes the bare checkout importable).
homestead-affairs>=0.3.0,<1.0
